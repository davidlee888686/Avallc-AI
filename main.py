"""Main entry point for the Avallc AI project.

This minimal example defines a `run_app` function that can be used
to start the application. For now, it just prints a message to the
console. Future versions could import additional modules (e.g. GUI,
service, GPU detection) to provide more advanced behaviour.
"""


def run_app() -> None:
    """Run the Avallc AI application.

    In this skeleton implementation the function simply outputs
    a message. You can expand this function to initialise models,
    start a GUI, or perform other tasks as your project evolves.
    """
    print("AVA Running")


if __name__ == "__main__":
    run_app()
